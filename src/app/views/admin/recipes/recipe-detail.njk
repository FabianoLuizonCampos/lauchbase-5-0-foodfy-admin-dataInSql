{% extends "user/layout.njk" %}

{% set pageId = "page-ad_recipe-detail" %}

<!-- STYLES PAGE -->
{% block styles %}
    <link rel="stylesheet" href="/styles/admin/header.css">   
    <link rel="stylesheet" href="/styles/admin/recipe-detail.css"> 
{% endblock styles %}     

<!-- PAGE CONTENT -->
{% block content %}
    <!-- HEADER -->
    {% include "admin/partials/page-header.njk" %} 

    <!-- CONTENT -->
    <div class="card-recipe">      
            <div class="card">

                <div class="card-type">
                    <h1>Receita: {{item.title}}</h1>
                    <a href="\admin\recipes\{{item.id}}\edit">Editar Receita</a>
                </div>

                <div class="card-image">
                    <img src="{{item.image}}" alt="">
                </div>

                <div class="card-title">
                    <h1>{{item.title}}</h1>
                    <p>por {{item.author}}</p>
                </div>

                <!-- INGREDIENTS -->
                <div class="card-ingredients"> 
                    
                    <div class="card-remove">
                        <h2>Ingredientes</h2> 
                        <p class="card-hide" id="0">Esconder</p>
                    </div> 
                    
                    <div class="card-text" id="card-text-ingredients">
                        <ul>
                        {% for ingredient in item.ingredients %}
                            <li>{{ingredient}}</li>
                        {% endfor %} 
                        </ul>   
                    </div>             
                    
                </div>

                <!-- PREPARATION -->
                <div class="card-preparation">

                    <div class="card-remove">
                        <h2>Modo de preparo</h2> 
                        <p class="card-hide" id="1">Esconder</p>
                    </div>                                    

                    <div class="card-text" id="card-text-preparation">
                        {% for preparation in item.preparations %}
                            <p>{{preparation}}</p>
                        {% endfor %}                        
                    </div>
             
                </div>

                <!-- INFORMATION -->
                <div class="card-information"> 

                    <div class="card-remove">
                        <h2>Informações adicionais</h2>                   
                        <p class="card-hide" id="2">Esconder</p>
                    </div>                 

                    <div class="card-text" id="card-text-information">
                        <p>{{item.information}}</p>
                    </div>
                
                </div>

            </div>  
        
    </div>
    

    <!-- FOOTER -->
    
{% endblock content %}
   
<!-- MODAL -->
{% block modal %}
{% endblock modal %} 

<!-- SCRIPTS -->
{% block scripts %}
    <script src="/scripts/recipe-detail.js"></script>
{% endblock scripts %} 